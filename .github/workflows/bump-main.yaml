name: Version bump (main)

on:
  # Manual trigger with options
  workflow_dispatch:
    inputs:
      skip-bump:
        description: "Skip version bump"
        required: false
        type: boolean
        default: false
      bump-type:
        description: "Bump type (leave auto for conventional commits)"
        required: false
        type: choice
        options:
          - auto
          - patch
          - minor
          - major
        default: auto

  # Auto-trigger on push, but allow skip
  push:
    branches:
      - main
    paths:
      - "packages/*/src/**"
      - "packages/*/package.json"

jobs:
  version-bump:
    # Skip if manually disabled OR if commit contains [skip bump]
    if: |
      !inputs.skip-bump &&
      (
        !github.event.head_commit ||
        (
          !contains(github.event.head_commit.message, '[skip bump]') &&
          !contains(github.event.head_commit.message, 'chore: bump versions') &&
          !contains(github.event.head_commit.message, 'version-bump-main')
        )
      )
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup and Build
        uses: ./.github/actions/setup
        with:
          node-version: "18.x"
          install-immutable: "true"
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Configure Git
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Ensure working tree is on main
        run: git checkout main

      - name: Sync with remote main
        run: |
          git fetch origin main
          git reset --hard origin/main

      - name: Delete existing PR branch if it exists
        run: |
          git push origin --delete version-bump-main 2>/dev/null || echo "Branch version-bump-main does not exist on remote, continuing..."

      - name: Detect current and target version
        id: version_meta
        run: |
          CURRENT=$(node -p "require('./lerna.json').version")
          if [[ "$CURRENT" == *"-rc."* ]]; then
            TARGET=${CURRENT%%-rc.*}
            echo "Detected release candidate version $CURRENT -> target $TARGET"
            echo "target=$TARGET" >> $GITHUB_OUTPUT
          else
            echo "No release candidate suffix detected (current=$CURRENT)"
            echo "target=" >> $GITHUB_OUTPUT
          fi

      - name: Decide if bump is required
        id: bump_control
        run: |
          if [ "${{ inputs.bump-type }}" != "auto" ] && [ -n "${{ inputs.bump-type }}" ]; then
            echo "Manual bump requested (${{ inputs.bump-type }})"
            echo "should=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          if [ -n "${{ steps.version_meta.outputs.target }}" ]; then
            echo "Graduating release candidate to ${{ steps.version_meta.outputs.target }}"
            echo "should=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "No bump required (auto mode with stable version)"
          echo "should=false" >> $GITHUB_OUTPUT

      - name: Bump Versions with Lerna
        if: ${{ steps.bump_control.outputs.should == 'true' }}
        run: |
          if [ "${{ inputs.bump-type }}" = "auto" ] || [ -z "${{ inputs.bump-type }}" ]; then
            if [ -n "${{ steps.version_meta.outputs.target }}" ]; then
              npx lerna version ${{ steps.version_meta.outputs.target }} --force-publish --no-push --no-git-tag-version --yes
            else
              npx lerna version --conventional-commits --force-publish --no-push --no-git-tag-version --yes
            fi
          else
            npx lerna version ${{ inputs.bump-type }} --no-push --no-git-tag-version --yes
          fi

      - name: Convert Lerna commit to staged changes
        if: ${{ steps.bump_control.outputs.should == 'true' }}
        run: git reset --soft HEAD~1 || true

      - name: Build Packages
        if: ${{ steps.bump_control.outputs.should == 'true' }}
        run: yarn build

      - name: Run Unit Tests
        if: ${{ steps.bump_control.outputs.should == 'true' }}
        run: yarn test:unit

      - name: Check for Changes
        if: ${{ steps.bump_control.outputs.should == 'true' }}
        id: check_changes
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Sync changes with latest main commit
        if: ${{ steps.bump_control.outputs.should == 'true' && steps.check_changes.outputs.has_changes == 'true' }}
        run: |
          # Commit the staged changes first
          git add .
          git commit -m "chore: version bump for release"

          # Fetch latest changes from main
          git fetch origin main

          # Merge with ours strategy to preserve version bump changes
          # This ensures our version bump commit stays on top
          git merge origin/main --strategy-option ours --no-edit || true

          # If there were conflicts, they're now resolved in favor of our changes
          # Ensure we're ahead of main for PR creation
          if git diff --quiet origin/main; then
            echo "No differences from origin/main, amending commit"
            git commit --amend --no-edit --allow-empty
          fi

      - name: Create or Update Pull Request
        if: ${{ steps.bump_control.outputs.should == 'true' && steps.check_changes.outputs.has_changes == 'true' }}
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "chore: bump versions for release [skip ci]"
          branch: "version-bump-main"
          title: "Production Release Version Bump"
          body: |
            ## 📦 Version Bump for Production Release

            This PR was automatically generated by the version bump workflow.

            ### Changes
            - Updated package versions using Lerna
            - Ran build

            **Merge this PR to trigger publishing to npm with `latest` tag.**
          base: "main"
          labels: |
            automated PR
            release:production
          delete-branch: true
